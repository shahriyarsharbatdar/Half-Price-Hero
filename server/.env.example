# Copy to .env — the server runs without a key (chef's tips fall back to rule-based)
ANTHROPIC_API_KEY=

# Shared admin secret — required to use /admin.html (catalogue PDF upload)
# and POST /api/admin/*. Generate one with: openssl rand -hex 32
SCRAPER_TOKEN=

# Optional overrides
PORT=3001
